

class User():
    def __init__(self, username, display_name, user_id, color, mod, subscriber, badges, badge_info, turbo):
        self.username = username
        self.display_name = display_name
        self.user_id = user_id
        self.color = color
        self.mod = mod
        self.subscriber = subscriber
        self.badges = badges
        self.badge_info = badge_info
        self.turbo = turbo